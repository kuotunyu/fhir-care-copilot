# reports/

`scripts/run_eval.py` 的輸出(PLAN.md M5/M6)。**這些檔案會進 git**——是可展示的
成果,不是暫存資料。

- `eval_results.json` — 單次 eval 執行的完整結果(每題判準 + 彙總指標)
- `model_comparison.md`（M6）— Gemini 與 gpt-5.4-mini 的比較報告,一頁式

跑法見 `scripts/run_eval.py` 的 docstring 或 `.claude/skills/run-eval/SKILL.md`。
