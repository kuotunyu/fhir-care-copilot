# Enable hooks: git config core.hooksPath scripts/git-hooks
#   (do NOT use `pre-commit install` -- it writes the venv path in cp950,
#    which breaks under this repo's non-ASCII path; see ADR 0002)
# Run all: uv run pre-commit run --all-files
# NOTE: keep this file ASCII-only -- pre-commit reads it with the system
# locale encoding (cp950 on zh-TW Windows), so non-ASCII bytes break it.
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ["--maxkb=500"]
      - id: check-merge-conflict
      - id: detect-private-key
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.22
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format
