# v0.2.1 Cryptography Release Blocker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve Dependabot alert #2 by updating only the transitive runtime dependency `cryptography` from `49.0.0` to exactly `50.0.0`, then restore the v0.2.1 release candidate to its full verified state.

**Architecture:** `cryptography` is introduced through `fhir-copilot -> google-genai[requests] -> google-auth -> cryptography`. Keep the application dependency declarations and product behavior unchanged; regenerate only the affected `uv.lock` package entry, record the release-blocker provenance, and rerun the existing release gates before tagging.

**Tech Stack:** uv lockfile, Python 3.11, pytest, Ruff, mypy, pre-commit, Docker, GitHub Actions/CodeQL/Dependabot, Hugging Face Space CPU mock/demo deployment.

## Global Constraints

- Work only in `C:/Users/3Hml/.codex/visualizations/2026/08/03/019fc611-3a18-76c0-a9fb-36ba3a46fa23/fhir-codeql-v021` on branch `codex/codeql-hardening-v0.2.1`.
- Baseline commit is `736d3aa90a167121fc2b747d9d8131417d3ffba7`; do not rewrite, squash, rebase, amend, or force-push history.
- Preserve the Feature Freeze: no product feature, UI, API, architecture, model, FHIR, or evaluation change.
- Do not read or display `.env` values, use real medical data, call paid APIs, use GPU, or describe synthetic data as real clinical data.
- Do not create the `v0.2.1` tag or GitHub Release until every local and remote gate is green and both CodeQL and Dependabot have zero open alerts.
- `cryptography` must resolve to exactly `50.0.0`; do not change `pyproject.toml`, `google-auth`, `google-genai`, or any other package version.
- The generated `uv.lock` diff may contain only the `cryptography` package version, source artifact URLs, hashes, sizes, and upload times required by `50.0.0`.
- Do not add a dependency-specific source-text test. The RED evidence is the authoritative open Dependabot alert plus the locked `49.0.0`; GREEN is the exact resolved version, lock consistency, existing full tests, and the closed alert after main integration.
- Every commit must be authored and committed only by `kuotunyu <61350295+kuotunyu@users.noreply.github.com>` with no co-author trailer.
- Keep GitHub Contributors limited to `kuotunyu`.

---

### Task 1: Apply and verify the narrow cryptography lockfile patch

**Files:**
- Modify: `uv.lock`
- Modify: `docs/superpowers/specs/2026-08-04-v0.2.1-codeql-security-hardening-design.md`
- Modify: `docs/superpowers/plans/2026-08-04-v0.2.1-codeql-security-hardening.md`
- Create: `docs/superpowers/plans/2026-08-04-v0.2.1-cryptography-release-blocker.md`

**Interfaces:**
- Consumes: Dependabot alert #2 (`GHSA-g6cj-pr64-35w5`, `CVE-2026-69247`) and the current `uv.lock` resolution `cryptography==49.0.0` through `google-auth==2.56.2`.
- Produces: one reviewed commit whose only dependency change is `cryptography==50.0.0`, with the original release design/plan recording the blocker and the requirement to repeat all exact-SHA gates.

- [ ] **Step 1: Record and verify RED evidence**

Use GitHub's Dependabot API to confirm alert #2 is open, severity is high, the vulnerable range is `>=44.0.0, <50.0.0`, and the first patched version is `50.0.0`. Run:

```powershell
uv tree --invert --package cryptography --depth 6
uv lock --check
```

Expected RED: the tree resolves `cryptography v49.0.0` through `google-auth`, and the lockfile is internally valid but security-vulnerable according to the open advisory.

- [ ] **Step 2: Prove the proposed resolution is narrow before writing**

Run:

```powershell
uv lock --upgrade-package 'cryptography==50.0.0' --dry-run
```

Expected: exactly `Update cryptography v49.0.0 -> v50.0.0`, with no other package update.

- [ ] **Step 3: Regenerate only the affected lock entry**

Run:

```powershell
uv lock --upgrade-package 'cryptography==50.0.0'
```

Inspect `git diff -- uv.lock`. Stop if any package other than `cryptography` changes or if `pyproject.toml` changes.

- [ ] **Step 4: Record release-blocker provenance**

Append a concise post-Task-9c release-blocker section to the existing security-hardening design and plan. It must name alert #2, the GHSA/CVE, the `google-auth` transitive path, the exact `49.0.0 -> 50.0.0` lock-only change, prohibited dependency drift, and the requirement to repeat local full gates, branch CI/CodeQL, exact-revision Hugging Face verification, main fast-forward CI/CodeQL, and zero-open CodeQL/Dependabot checks before Task 10.

- [ ] **Step 5: Verify GREEN dependency state**

Run:

```powershell
uv lock --check
uv sync --locked --all-extras --dev
uv tree --invert --package cryptography --depth 6
uv run python -c "import cryptography; assert cryptography.__version__ == '50.0.0'; print(cryptography.__version__)"
```

Require `cryptography v50.0.0`, no lock drift, and no other dependency version change from the baseline.

- [ ] **Step 6: Run existing backend quality gates**

Run the repository's existing full backend pytest, Ruff, mypy, pre-commit, and `git diff --check` commands exactly as documented in the main v0.2.1 hardening plan. Do not add tests or broaden product scope.

- [ ] **Step 7: Inspect scope and commit once**

Require the tracked diff to contain only `uv.lock` and the three approved plan/design provenance files. Confirm `pyproject.toml` is unchanged, the only package version delta is `cryptography 49.0.0 -> 50.0.0`, and Git identity is exact. Commit once with:

```powershell
git -c user.name=kuotunyu -c user.email=61350295+kuotunyu@users.noreply.github.com commit -m "fix: update vulnerable cryptography lockfile"
```

- [ ] **Step 8: Restore all release gates before Task 10**

After task review approval, repeat the existing full local CPU/mock tests and Docker smoke; push the patch branch once; inspect exact-SHA branch CI and CodeQL logs; publish and byte-verify exactly one new Hugging Face CPU mock/demo revision; fast-forward main by one commit and push once; inspect exact-SHA main CI and CodeQL logs. Require Dependabot alert #2 to become `fixed` automatically with no dismissal metadata, alert #1 to remain fixed, the open Dependabot endpoint to return `[]`, the open CodeQL endpoint to return `[]`, Contributors to return only `kuotunyu`, and all worktrees to be clean. Do not tag or release on any failure.
