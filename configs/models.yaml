# Provider -> 模型 id 對應。只改這裡,不改程式——上游改模型 id 時單點修改。
# GeminiProvider/OpenAIProvider 的 model_id 由 providers/factory.py 從這裡讀入
# (不是寫死在 class 裡),換模型只要改這個檔案。

providers:
  mock:
    model_id: mock-deterministic
    # CI 與離線開發預設 provider;不打任何外部 API。

  gemini:
    # 歷史外部-provider eval 使用這個明確版本。不要改成 latest alias，否則模型行為、
    # 成本與 committed artifact 會失去對應；選型證據見 reports/injection_ab.md。
    model_id: gemini-3.1-flash-lite
    sdk: google-genai
    api_key_env: GEMINI_API_KEY
    # 預設不跨環境 fail over。若新增備援，只列已獨立驗證且允許共用預算的 env name。
    backup_api_key_envs: []

  openai:
    model_id: gpt-5.4-mini
    sdk: openai (Responses API)
    api_key_env: OPENAI_API_KEY

default_provider: mock
